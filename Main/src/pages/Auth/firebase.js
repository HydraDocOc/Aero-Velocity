// Thin wrapper to reuse the app-level Firebase setup while satisfying the
// requested co-located file structure for the Auth page.
export { auth, googleProvider, getOrCreateRecaptcha } from '../../firebase';


